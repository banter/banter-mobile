export default class TagItem {
    constructor({
      id,
      type,
      value,
      imageUrl,
    }) {
      this.id = id;
      this.type = type;
      this.value = value;
      this.imageUrl = imageUrl;
    }
  }
